micronaut:
  application:
    name: distributed-config
  config-client:
    enabled: true
consul:
  client:
    registration:
      enabled: false
    defaultZone: ${CONSUL_HOST:localhost}:${CONSUL_PORT:8500}
